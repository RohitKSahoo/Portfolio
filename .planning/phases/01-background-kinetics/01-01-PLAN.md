---
wave: 1
depends_on: []
files_modified: [src/components/effects/DotGrid.tsx]
autonomous: true
---

# Plan 01-01: Background Infrastructure & Proximity Glow

Implement the core rendering engine for the kinetic grid and the interactive proximity glow effect.

## Objectives
- Refactor `DotGrid.tsx` to align with Phase 1 UI-SPEC.
- Optimize the Canvas draw loop for 60FPS.
- Implement soft-glow proximity color interpolation.

## Proposed Changes

### Interactions & Physics
- Update proximity radius to 150px (token-aligned).
- Implement exponential falloff for the dot glow color.

### Component Logic
- Update `DotGridProps` default values.

## Tasks

<task id="01-01-01">
  <action>
    Update 'DotGridProps' in 'src/components/effects/DotGrid.tsx' to include 'dotSize: 2' and 'gap: 32' as defaults.
  </action>
  <read_first>
    - src/components/effects/DotGrid.tsx
  </read_first>
  <acceptance_criteria>
    - DotGridProps interface has 'dotSize' defaulted to 2
    - DotGridProps interface has 'gap' defaulted to 32
  </acceptance_criteria>
</task>

<task id="01-01-02">
  <action>
    Refactor the 'draw' function in 'DotGrid.tsx' to use a sharper proximity falloff. Update fillStyle to interpolate between '#1a1a1a' (base) and '#f43f5e' (accent) based on distance.
  </action>
  <read_first>
    - src/components/effects/DotGrid.tsx
    - .planning/phases/01-background-kinetics/01-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - 'draw' function calculates 'dist' from pointerRef
    - 'fillStyle' uses interpolated values based on proximity
    - Animation remains smooth at 60FPS
  </acceptance_criteria>
</task>

## Verification
- Run 'npm run dev' and verify dots glow when mouse is nearby.
- Check console for any Canvas performance warnings.
